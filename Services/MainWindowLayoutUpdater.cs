namespace SoulBuddy.Services;

// Layout changes are implemented directly in MainWindow. This class remains
// only to avoid breaking references from older builds; it intentionally does
// not attach global Avalonia layout handlers.
internal static class MainWindowLayoutUpdater
{
}
